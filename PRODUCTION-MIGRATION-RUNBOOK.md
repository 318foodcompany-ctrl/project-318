# Production Migration Runbook

This is the authoritative migration order for the integrated launch candidate. Apply to a backed-up disposable staging project first. Production execution requires account-owner approval and is not part of this release branch.

| # | Forward migration | Purpose and prerequisites | Rerunnable | Validation query (must return non-null/expected rows) | Rollback file and limitation | Risk | Required environment |
|---:|---|---|---|---|---|---|---|
| 1 | `supabase/baseline-schema.sql` | Base leads, website content, and menu schema; blank database | Yes; rerun-safe DDL | `select to_regclass('public.leads'), to_regclass('public.website_content'), to_regclass('public.menu_items');` | None; restore backup | High if reversed | Database operator access only |
| 2 | `supabase/website-settings.sql` | Public website settings; requires #1 | Yes | `select to_regclass('public.website_settings');` | None; restore backup | Low | Database operator access only |
| 3 | `supabase/quote-internal-notes.sql` | Private notes on leads; requires #1 | Yes | `select column_name from information_schema.columns where table_schema='public' and table_name='leads' and column_name='internal_notes';` | None; forward correction only | Low | Database operator access only |
| 4 | `supabase/booking-calendar.sql` | Bookings and calendar RPCs; requires leads | Yes | `select to_regclass('public.bookings');` | None; restore backup | High | Database operator access only |
| 5 | `supabase/customer-crm.sql` | Customers, activity history, and administrator rule; requires #1 and #4 | Yes | `select to_regclass('public.customers'), to_regclass('public.customer_activities'), to_regprocedure('public.crm_is_admin()');` | `supabase/customer-crm-rollback.sql`; can remove CRM structures and must not run with business records | High | Database operator access; staging admin JWT for verification |
| 6 | `supabase/customer-crm-status-fix.sql` | Correct quote-status policy and activity behavior; requires #5 | Yes | `select policyname from pg_policies where schemaname='public' and tablename='leads' and policyname='CRM administrators can update quote status';` | `supabase/customer-crm-status-fix-rollback.sql`; restores older behavior | Medium | Database operator access; staging admin JWT |
| 7 | `supabase/invoicing-payments.sql` | Financial ledger, numbering, and payments; requires #5 and #4 | Yes | `select to_regclass('public.invoices'), to_regclass('public.invoice_line_items'), to_regclass('public.payments');` | `supabase/invoicing-payments-rollback.sql`; feature-disable only and intentionally preserves financial records | Critical | Database operator access; all `INVOICE_TEST_*` values for validation |
| 8 | `supabase/marketing-attribution.sql` | Visitor/session/touch attribution; requires leads, CRM, invoices | Yes | `select to_regclass('public.marketing_visitors'), to_regclass('public.marketing_sessions'), to_regclass('public.marketing_touchpoints');` | `supabase/marketing-attribution-rollback.sql`; preserves existing attribution rows | Medium | Database operator access; staging public URL/anon key for journey test |
| 9 | `supabase/marketing-funnel-reporting.sql` | Administrator funnel RPC; requires #8 | Yes | `select to_regprocedure('public.marketing_quote_funnel(date,date,text)');` | None; restore prior function definition | Low | Database operator access; staging admin JWT |
| 10 | `supabase/marketing-spend-roas.sql` | Spend and directly attributed ROAS; requires #8 and #9 | Yes | `select to_regclass('public.marketing_spend');` | None; restore backup/function definition | Medium | Database operator access; staging admin JWT |
| 11 | `supabase/website-settings-admin-hardening.sql` | Replace broad authenticated settings writes with admin-only policy; requires #5 and #2 | Yes | `select policyname,roles from pg_policies where schemaname='public' and tablename='website_settings';` | None; forward correction only | Low | Database operator access; ordinary and admin staging JWTs |
| 12 | `supabase/migrations/20260722190000_admin_rls_hardening.sql` | Harden leads/content/menu RLS; requires #5 | Yes | `select tablename,policyname from pg_policies where schemaname='public' and tablename in ('leads','website_content','menu_items') order by 1,2;` | None; never restore broad authenticated access | Low | Database operator access; anon/ordinary/admin staging JWTs |
| 13 | `supabase/migrations/20260722200000_website_images_storage.sql` | Website-images bucket and storage policies; requires #5 | Yes | `select name,public from storage.buckets where id='website-images';` | None; preserve stored objects and correct policies forward | Medium | Database operator access; anon/ordinary/admin staging JWTs |
| 14 | `supabase/release-1-lead-automation.sql` | Idempotent lead intake, consent, delivery evidence, and rate limits; requires #5, #8, and hardened RLS | Yes | `select to_regclass('public.lead_submission_idempotency'), to_regclass('public.marketing_consent_history'), to_regclass('public.lead_email_deliveries');` | `supabase/release-1-lead-automation-rollback.sql`; non-destructive feature disable | Medium | Database operator access; lead/email test-mode variables for application validation |
| 15 | `supabase/release-2-conversion-content.sql` | Testimonials, gallery, FAQ, and event types; requires #5 and #13 | Yes | `select to_regclass('public.conversion_items'), to_regclass('public.gallery_items'), to_regclass('public.faq_items'), to_regclass('public.event_types');` | `supabase/release-2-conversion-content-rollback.sql`; disables access and retains content | Low | Database operator access; anon/admin staging JWTs |
| 16 | `supabase/release-3-sales-platform.sql` | Pipeline, proposals, portal, documents, and follow-ups; requires #4, #5, #7, #14, #15 | Yes | `select to_regclass('public.sales_opportunities'), to_regclass('public.proposals'), to_regclass('public.customer_portal_tokens'), to_regclass('public.follow_up_messages');` | `supabase/release-3-sales-platform-rollback.sql`; refuses when business records exist | High | Database operator access; all `CRM_TEST_*` values plus staging admin/test identities |
| 17 | `supabase/launch-readiness-hardening.sql` | Atomic/recoverable follow-up claims; requires #16 | Yes | `select to_regprocedure('public.sales_claim_due_followups(integer)');` | `supabase/launch-readiness-hardening-rollback.sql`; narrowly reverts queue claim behavior | Medium | Database operator access; staging service role and `FOLLOW_UP_CRON_SECRET` for runner validation |
| 18 | `supabase/release-4-ai-marketing.sql` | AI drafts, campaigns, templates, sequences, provider events, consent, and suppression; requires #14, #16, #17 | Yes | `select to_regclass('public.marketing_campaigns'), to_regclass('public.marketing_ai_content'), to_regclass('public.marketing_suppressions'), to_regclass('public.marketing_email_events');` | `supabase/release-4-ai-marketing-rollback.sql`; refuses with business or compliance history | High | Database operator access; all `CRM_TEST_*` values and AI/email/webhook/scheduler test variables |

Before every step: confirm the staging project reference, take/confirm a backup, inspect the SQL transaction boundary, and record start/end timestamps. After every step: capture the expected verification above, inspect Supabase logs, and stop on any unexpected row count, policy, function, or permission change.

Static integration review found no duplicate release filenames, duplicate fixed-name indexes, unresolved function ordering, or missing parent migration. Rerunnable policy migrations drop/recreate their fixed policy names or guard dynamic creation. The sequence places `crm_is_admin()` before every dependent policy, storage hardening before Release 2/3 content use, Release 3 before queue hardening, and Release 4 after the hardened queue. Anonymous access is limited to intentionally public published content, lead submission operations, public storage reads, and opaque-token portal/unsubscribe operations; private management tables remain RLS-protected. Rollbacks for invoicing, Release 3, and Release 4 are not disaster-recovery substitutes and must never be forced around their business-data guards.

Required configuration for staging validation: `CRM_TEST_SUPABASE_URL`, `CRM_TEST_ANON_KEY`, `CRM_TEST_ADMIN_TOKEN`, `CRM_TEST_SERVICE_ROLE_KEY`, `INVOICE_TEST_SUPABASE_URL`, `INVOICE_TEST_ANON_KEY`, `INVOICE_TEST_ADMIN_TOKEN`, and `INVOICE_TEST_SERVICE_ROLE_KEY`. Never commit their values.
